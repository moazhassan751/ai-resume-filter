name: Deploy with Secret
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      # will be provided via repository secrets
      GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push images
        run: |
          echo "Using GOOGLE_API_KEY=${{ secrets.GOOGLE_API_KEY }}"
          # build/push steps here (customize as needed)
      - name: Deploy (example)
        run: |
          echo "Deploy steps go here — use your infra tooling to deploy images and set runtime envs"
